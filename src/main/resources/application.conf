ktor {
    deployment {
        username = ocholoko888
        username = ${?USERNAME}
        password = ASdfrewq6677!!
        password = ${?PASSWORD}
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}